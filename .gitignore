.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Derived data: clones, SQLite, measurement output. The source is upstream; the index is in Milvus.
data/
evals/results/*.json
# Golden sets are specific to the indexed repo and contain internal file paths; only the template is published.
evals/golden.*.jsonl
!evals/golden.example.jsonl
